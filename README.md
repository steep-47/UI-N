# UI-N

面向手机端 SillyTavern 的小说阅读式界面插件。

## 功能

- 日间、夜间与跟随系统三种显示模式
- 小说式正文排版，可调字号、行距和左右留白
- 保留层数、缓存命中、思考时间与 Token 数
- 保留消息菜单和编辑按钮
- 用户消息可压缩为低干扰指令块
- 输入栏旁提供一键进入或退出阅读模式
- 不修改聊天内容，不影响编辑、滑动重生成和其他插件逻辑

## 安装

将本仓库地址粘贴到 SillyTavern 的“扩展 → 安装扩展”中。若使用压缩包，请解压到 SillyTavern 的第三方扩展目录后重启。

## 使用

点击输入栏左侧的书本按钮切换沉浸阅读。详细选项位于“扩展设置 → UI-N”。

## 兼容说明

插件读取 SillyTavern 原生的消息层数、计时和 Token 显示节点，并尝试识别包含“缓存命中”的状态节点。不同版本的 Memo-N 若使用了其他 DOM 类名，缓存数据仍可能需要一次针对性适配。

### 0.5.36 日间思考强调文字拉开层级

- 根据明暗两张截图重新对比：夜间模式中粗体/重点词已经明显，问题主要出在日间模式
- 日间思考正文保持浅灰辅助层级，但 strong/b 重点词改为明显更深的灰黑，并强制 700 字重
- 重点词与普通思考文字不再只靠细微色差区分
- 夜间模式不改，正文、字号、行距、布局和折叠逻辑均不动

### 0.5.35 思考文字层级增强

- 只调整思考区文字颜色，不改字号、行距、布局或正文
- 日间模式：思考正文改为更浅的灰褐色，和正式正文拉开明显层级；粗体/标题保留稍深一级
- 夜间模式：思考正文进一步压暗，粗体/“思考中”标题略亮，避免整块文字与正式正文看成同一层
- 覆盖酒馆对思考区 em/i 等内部节点的独立颜色，保证整段思考文字统一处于辅助层级
- 左侧细线同步使用思考文字色，不改思考区结构和折叠功能

### 0.5.34 日夜切换移入魔法棒菜单

- 移除输入栏左侧单独的 UI-N 日间/夜间切换按钮
- 将主题切换作为普通菜单项加入酒馆原生魔法棒 `#extensionsMenu`
- 菜单项会根据当前模式显示“切换到日间模式”或“切换到夜间模式”，并用太阳/月亮图标提示
- 若魔法棒菜单比 UI-N 晚加载，会等待原生菜单出现后再挂载，不创建替代菜单
- 插件停用时移除该菜单项；重新启用时按原生菜单重新加入
- 仅改变切换入口位置，不修改魔法棒原有功能、正文排版或其他 UI 结构

### 0.5.33 恢复原生消息按钮显示

- 继续排查 0.5.32 后按钮全消失的问题：高优先级布局规则仍把整个 `.ch_name` 隐藏，而酒馆原生 `.mes_buttons` 就在这个容器里面
- 现在保留 `.ch_name` 容器，只隐藏角色名等非按钮元素；原生按钮继续留在酒馆原位置
- 不再新增、不再搬运、不再强制显示按钮，酒馆原来该显示哪两个就显示哪两个
- “…”更多菜单继续依赖酒馆原生 DOM 与事件逻辑，不由 UI-N 接管
- 清理旧 `.ntu-actions` 遗留样式
- 正文、元信息、用户消息、思考区等其它样式保持不变

### 0.5.32 恢复酒馆原生消息按钮逻辑

- 定位到截图里多出第三个“旗帜”按钮的原因：UI-N 之前对所有 `.mes_button` 强制 `display`，把酒馆原本隐藏的书签按钮也显示出来了
- 同时 UI-N 曾把整组 `.mes_buttons` 从酒馆原位置搬到自建 `.ntu-actions`，会破坏“…”更多菜单依赖的原生结构/定位，因此出现菜单内容或点击异常
- 现在完全取消按钮搬运，也不再强制 `display / visibility / position`
- 酒馆原本显示几个按钮就显示几个，原本隐藏的继续隐藏；“…”菜单和其中功能继续使用酒馆原生逻辑
- UI-N 只保留颜色层面的适配，不新增按钮、不改按钮功能、不接管菜单
- 正文排版与 0.5.31 的元信息/用户消息/思考区视觉调整保持不变

### 0.5.31 消息辅助区细化

- 正文排版保持 0.5.30 不动，只整理截图中“层数/缓存/耗时/Token、操作按钮、用户消息、思考折叠条”这一组辅助 UI
- 元信息行高度与间距收紧，弱化成类似页边批注的信息层，不再抢正文
- 三个操作按钮缩到 29px，并改为默认透明，仅交互时显出背景/边框
- 用户消息由整条大卡片改成紧凑注记块，短消息不再撑满整行
- “思考了一会”去掉整块灰底，只保留小型折叠控制；展开内容改为细左线 + 透明背景
- 不修改正文字号、行距、段距、左右留白、主题颜色和功能逻辑

### 0.5.30 按实际生效比例校准正文排版

- 0.5.29 证明参数终于真正生效，但 32 / 2.50 明显过大；此前多版截图其实一直停留在旧的 19 / 1.95
- 依据同为 691×1536 的截图实测：19px 时可见字高约 29px，32px 时约 48–49px，参考图约 34–35px，因此目标字号校准为 23px
- 参考图相邻正文行中心距约 80–81px，结合实际缩放比例将行高设为 2.10
- 参考图段落额外间隔约 29px，段间距校准为 .75em
- 字体、字重、字距、首行 2em 缩进与左右留白保持不变
- typographyPresetVersion 升到 2，本次升级一次性写入 23 / 2.10，之后仍保留手动调整

### 0.5.29 修复正文参数未真正生效

- 根据截图反推确认：0.5.28 画面仍接近旧的 27 / 2.25，而不是目标 32 / 2.50
- 原因不是后面的 CSS 再覆盖，而是旧的已保存设置优先于 defaults；此前迁移又要求字号、行距、左右留白同时精确匹配，条件很容易失效
- 新增一次性 typographyPresetVersion：升级到本版时强制将正文字号写入 32、行距写入 2.50，一次生效后不再覆盖后续手动调整
- 同时将 CSS 根变量兜底值同步为 32 / 2.50
- 段间距 1em、首行缩进 2em、字距 0、思源黑体及左右留白保持不变
- 仅修正文文字参数实际生效问题，不改颜色、UI 或其他功能

### 0.5.28 按参考截图像素比例重做正文尺寸

- 不再凭感觉调参，按两张同为 691×1536 的截图直接比像素：当前字高约 28–29px，参考约 33–35px
- 默认字号从 27 调整为 32；参考图行距约 80px，因此行高设为 2.50（32×2.50≈80）
- 段间距设为 1em，对应参考图约 110px 的段落起始间隔
- 字体、字重、字距、首行 2em 缩进和左右留白保持不变
- 正文字号滑块上限扩到 36，行距上限扩到 2.80，避免新默认值被旧滑块上限卡住
- 0.5.27 默认 27 / 2.25 / 24 自动迁移到 32 / 2.50 / 24；自定义参数不覆盖

### 0.5.27 参考图字号与行距对齐

- 默认正文字号从 23 调整为 27，更接近参考图的实际字高
- 默认行距从 2.05 调整为 2.25，补足参考图更舒展的纵向节奏
- 字距收紧为 0，减少当前版本偏松的电子阅读感
- 首行缩进、段间距与左右留白保持不变
- 0.5.26 默认参数 23 / 2.05 / 24 会自动迁移到 27 / 2.25 / 24；用户自定义参数不强制覆盖
- 仅修改正文文字参数，其他 UI 与功能不动

### 0.5.26 正文尺寸与参考图对齐

- 对比同为 691×1536 的截图，参考图正文实际约比当前大 20% 左右，因此默认字号从 19 调到 23
- 保持思源黑体 / Noto Sans CJK、400 字重与现有字形风格不变
- 行距默认调整为 2.05，并恢复由 UI-N“正文行距”设置实际控制
- 段间距从 1.35em 收到 0.80em，避免放大字号后段落空隙过大
- 首行 2em 缩进与左右留白保持不变；旧默认 19 / 1.95 / 24 会自动迁移，自定义过的参数不覆盖
- 仅修改正文文字参数，其他 UI 与功能不动

### 0.5.25 修复正文被旧宋体规则覆盖

- 定位到正文实际仍显示宋体的原因：后面的高优先级 `#sheld #chat .mes .mes_text` 规则仍强制使用 Noto Serif / Source Han Serif
- 将该最终覆盖规则同步改为思源黑体 / Noto Sans CJK，并保留参考图的 400 字重、2.05 行高与 .018em 字距
- 仅修改正文文字配置，其他 UI、布局、输入栏、世界书和功能逻辑不动

### 0.5.24 思源黑体正文排版

- 正文字体改为思源黑体优先：Source Han Sans SC / Noto Sans CJK SC / Noto Sans SC
- 保留 2 字符首行缩进，正文改为更接近参考图的舒展黑体排版
- 行高调整为 2.05，段间距调整为 1.35em，字距轻微放开
- 仅修改正文阅读排版，不动 UI 布局、世界书、输入栏和其他稳定功能
- 稳定备份 `stable-v0.5.16` 保持不变

### 0.5.16 世界书布局恢复原版

- 确认空白高度由 UI-N 对 Select2 / 世界书布局的覆盖引入，原版 TauriTavern 2.3.0 本身没有该问题
- 删除 0.5.13–0.5.15 对世界书选择器的高度、宽度、padding、display、flex、overflow 等布局接管
- Select2 恢复完全使用 TauriTavern / SillyTavern 原生布局，只保留 UI-N 的颜色覆盖
- 将 Select2 内联搜索框排除出 UI-N 通用 textarea 样式，避免再次改变原控件尺寸

### 0.5.15 TauriTavern 世界书空白行修正

- 定位到 TauriTavern 2.3.0 的移动端布局：`#wiTopBlock` 改成纵向后，`#WIMultiSelector` 仍保留 `.flex1 { flex: 1 }`
- 取消世界书全局选择器在纵向主轴上的 flex-grow，避免整块区域被拉高
- 让标题、选择器容器与 Select2 都按实际内容高度收缩，不再多出大块空白

### 0.5.14 世界书选择器空行微调

- 修复 Select2 内联搜索 textarea 被通用输入框样式撑高，导致“已启用的世界书”多出一整行空白
- 将世界书空选择状态收紧为单行高度，同时保留多选世界书换行能力
- 增加 TauriTavern 包装结构下的世界书 Select2 高度兜底

### 0.5.13 世界书选择器与黑色按钮修正

- 重做 0.5.12 的 Select2 覆盖，修复“已启用的世界书”被撑成大块空白的问题
- 强制全局世界书选择器的内联搜索框保持单行紧凑高度
- 清理欢迎页、最近聊天操作、临时聊天以及正文快捷按钮残留的黑色填充
- 保留 checkbox、range 与世界书下拉菜单的 UI-N 配色统一

### 0.5.12 表单控件与世界书细节统一

- checkbox 改用 UI-N 自身的面板、边框与强调色，移除底层主题残留的紫色勾选效果
- range 滑块轨道与圆点统一到 UI-N 色板，不再继承原主题强调色
- 直接接管 Select2 生成后的真实控件层，统一输入面、下拉列表、选中项与搜索框
- 修复“已启用的世界书（全局有效）”外层深色粗框与异常胶囊样式
- 世界书编辑区的启用状态与条目边框同步 UI-N 色板

### 0.5.11 Opt-N 翻页控件避让

- 识别 Opt-N 的 `.yiyi-choice-panel`，只对带选项面板的 AI 消息增加底部控件空间
- 将酒馆原生的 `1/1` 与左右滑动箭头留在独立底部区域，不再压住选项说明文字
- 普通消息不增加额外空白，不影响 0.5.10 的滚动与输入栏布局

### 0.5.10 滚动手感与底部留白

- 键盘打开时底部缓冲区改为固定的小留白，不再重复叠加一整块输入栏高度
- 正文恢复浏览器原生触摸滚动与惯性，不再用脚本逐帧接管 scrollTop
- 用户一开始拖动正文就停止自动定位，避免键盘动画期间把页面反复拉回底部
- 收起键盘后仍按悬浮输入栏真实高度保留必要避让，不重新引入遮挡

### 0.5.9 输入状态布局重构

- 输入时不再让输入栏悬浮覆盖正文，改回酒馆原生的正文与输入栏纵向布局
- 键盘弹出后正文自动占据输入栏上方的剩余空间，从结构上消除遮挡
- 收起键盘后恢复悬浮输入栏与沉浸阅读效果

### 0.5.8 输入时自动避让

- 输入框获得焦点后自动将聊天区滚至真实底部，把末尾正文与选项抬到输入栏上方
- 针对 Android 键盘分阶段弹出的过程多次校准位置，避免第一次调整后又被键盘覆盖
- 用户主动拖动正文时立即停止自动定位，随后仍可自由浏览

### 0.5.7 强制滚动缓冲区

- 在聊天末尾插入真实空白节点，不再依赖浏览器对底部内边距的滚动计算
- 输入框聚焦时直接接管正文纵向拖动，兼容会阻止原生滚动的移动端酒馆版本
- 点击选项与按钮仍保持原交互，只有形成滑动手势后才滚动正文

### 0.5.6 键盘可视区域修复

- 键盘弹出时按 Android 实际可见高度缩短聊天滚动区域
- 底部留白不再落入键盘背后，正文和选项可在输入栏上方自由上下滑动
- 保留输入栏独立滚动与动态正文缓冲区

### 0.5.5 正文底部滚动缓冲区

- 在最后一条正文后增加随输入栏高度变化的真实可滚动留白
- 键盘弹出或输入栏增高时同步扩大缓冲区，末尾正文与选项不再被悬浮输入栏盖住
- 收起沉浸操作栏后自动缩小留白，不影响正常小说阅读

### 0.5.4 Android 输入缓冲区修复

- 改用脚本按真实文字高度扩展输入区，兼容不支持自动高度的 Android WebView
- 达到高度上限后接管输入框内的纵向手势，滑动长文本时不再被聊天页截走
- 键盘高度变化、获得焦点及文字变化时都会重新校准输入区高度

### 0.5.3 可滚动编辑缓冲区

- 文本区随内容逐步增高，最大约为屏幕高度三分之一
- 达到上限后固定卡片高度，文本可在输入区内自由上下滑动
- 取消失焦强制压缩，返回输入栏时仍保留此前的阅读位置与编辑空间
- 输入区滚动与聊天正文滚动隔离，避免滑动长文本时带动页面

### 0.5.2 输入栏视觉优化

- 左侧功能键与右侧发送键重新分区，不再全部挤在右侧
- 文本区最高约四行，减少对正文和选项的遮挡
- 缩小按钮与卡片体积，减轻阴影、边框和圆角
- 输入区滚动条改为低干扰细线

### 0.5.1 输入栏结构隔离

- 启用时实际拆分文本区与按钮区，不再依赖第三方 UI 的原有层级
- 修复输入框被压成竖线、按钮堆在左侧及卡片空白过高
- 关闭 UI-N 时恢复 SillyTavern 原始输入栏节点位置

### 0.5.0 ChatGPT 式移动输入栏

- 输入内容与功能按钮分为上下两层，避免横向挤压
- 输入栏作为圆角卡片悬浮于底部，不再占用正文布局高度
- 手机键盘弹出时自动贴在键盘上方，收起后回到底部
- 长文本限制在输入区内部滚动，不再撑满屏幕
- 通过剧情选项打开输入框时，即使处于沉浸模式也可正常显示

### 0.4.3 输入栏收起修复

- 输入框失焦后自动收回紧凑高度，编辑时最高仅展开约三行
- 隐藏操作栏时输入区高度归零，不再留下大块空白
- 隐藏输入栏时主动取消输入焦点并收起手机键盘

### 0.4.2 输入栏兼容修复

- 修复隐藏操作栏后从剧情选项“填入输入框”时，部分 UI 主题把输入框挤成竖排并异常增高
- 点击扩展提供的可交互选项卡时，不再同时触发阅读操作栏显隐

### 0.4.1 完整日夜主题

- 修复沉浸模式下消息菜单与铅笔编辑按钮消失
- 夜间模式同步顶部工具栏颜色
- 扩大“缓存命中”状态的识别范围
- 退出沉浸模式时恢复按钮原位置和原界面结构
- 提高夜间模式下消息操作按钮与输入栏图标的辨识度
- 提高消息页数和左右滑动箭头的辨识度
- 开启 UI-N 时使用独立的消息、字体、头像显隐与输入栏布局
- 切换第三方 UI 主题时不再继承其字号、消息宽度和排列方式
- 关闭 UI-N 后仍完整使用原主题
- 日间与夜间模式覆盖顶部栏、左右抽屉、设置页、角色与世界书列表、弹窗、表单和滚动条
- 所有酒馆页面统一使用 UI-N 色板，不再只有聊天正文进入沉浸模式
- 修正书本按钮逻辑：不再关闭 UI-N，改为直接切换完整日间/夜间主题
- 接管酒馆通用主题变量，统一原生页面与第三方扩展的颜色
- 清除底层主题遗留的渐变标题、文字描边和模糊阴影
- 修复提示词管理器列表与思考折叠栏在日间模式下的对比度
- 修复普通标签被错误加上块状背景
- 清除消息操作按钮的立体阴影，改为扁平轻边框
- 清除对话补全预设标题栏的黑色渐变残留
- 统一接管高级格式化页的系统提示词、停止字符串、分词器与推理等分组标题
- 清除设置页通用标题组件从第三方主题继承的渐变、阴影和文字描边
- 修复输入栏菜单与扩展菜单在日间、夜间模式下背景和文字对比度异常
- 统一两类弹出菜单的背景、边框、图标、菜单项及悬停状态
- 清除菜单内部继承的透明度与文字填充色，提升日间和夜间文字对比度
- 清除扩展菜单各选项内部残留的块状填充，统一为单一菜单底色
- 为日间与夜间菜单设置独立柔和文字色，降低黑白高对比造成的刺眼感
- 依据 W3C 可读性标准重建统一日夜色板，日间全面取消纯白及近白填充
- 正文、菜单、面板、输入框和浮层统一使用同一套四级表面颜色
- 新增小说式单击交互：轻触正文切换顶部工具栏与底部输入栏
- 上下滑动、选择文字及点击链接、选项或消息按钮时不会误触切换
- 操作栏显隐不改变正文滚动位置，消息统计与右侧菜单、旗帜、编辑按钮始终保留

当前版本：0.5.24

## License

MIT
